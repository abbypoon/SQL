WITH TRANSACTION AS (
SELECT *,   
REPLACE(SORT_CODE, '-', '') AS SORT__CODE,
CONCAT('GB',CHECK_DIGITS,SWIFT_CODE,SORT__CODE,ACCOUNT_NUMBER) AS IBAN
FROM PD2023_WK02_TRANSACTIONS AS T
LEFT JOIN PD2023_WK02_SWIFT_CODES AS S
ON T.BANK=S.BANK)

SELECT TRANSACTION_ID, IBAN
FROM TRANSACTION
